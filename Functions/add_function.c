#include <stdio.h>

// Function to add two numbers
int add(int a, int b) {
    return a + b;
}

int main() {
    int x = 10, y = 5;
    printf("Sum is %d\n", add(x, y));// calling function
    return 0;
}
